# Accessibility hooks

Hooks that surface platform a11y settings to React components.

- `useReducedMotion.ts` respects iOS Reduce Motion / OS reduce-motion preference
- `useAccessibilitySettings.ts` is prototype-level toggles (long-press-anywhere and others)
