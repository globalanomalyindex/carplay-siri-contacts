# Tokens

Design tokens as CSS custom properties. Imported once by `src/index.css`.

- `motion.css` is named easing curves and durations
- `color.css` is the Siri palette, glass treatment, semantic action colors
- `spatial.css` is sizes, radii, and thresholds
